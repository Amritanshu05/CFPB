# Copy this file to .env and fill in your API keys.
# .env is gitignored — never commit real keys.

# OpenAI API key (for generation stage, optional)
OPENAI_API_KEY=

# Anthropic API key (for generation stage, optional)
ANTHROPIC_API_KEY=
